package dev.shaaf.waver.config.llm;

/**
 * Represents supported Large Language Model providers for tutorial generation.
 * Each provider has specific configuration requirements and authentication methods.
 */
public enum LLMProvider {
    OpenAI,
    Gemini
}
